import { StyleSheet } from "react-native"

export default function MenuButton({ onPress, text, icon, }) {
    return (
        <></>
    )

};

const styles = StyleSheet.create({
    container: {

    },
    textStyle: {

    },
    iconStyle: {

    }
});